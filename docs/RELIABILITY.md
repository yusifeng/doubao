# Reliability

## 开发期稳定性基线

- Android/iOS/Web 任一调试通路异常时，优先定位环境问题与命令路径。
- 对外部依赖下载失败（如 Expo Go）采用“可重试 + 明确代理条件”策略。
- 所有修复应优先保持默认开发命令稳定：`pnpm run android/ios/web`。

## 运行与调试约束

- 环境问题与代码问题分离排查，避免误判为业务缺陷。
- 发现系统级异常（如模拟器配置漂移）需记录到文档，避免重复踩坑。
- 重大调试路径变化（脚本/命令）必须在 docs 中同步说明。
